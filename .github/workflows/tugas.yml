name: Keep Git Green

on:
  schedule:
    - cron: '0 8 * * *'  # 15:00 WIB
    - cron: '0 1 * * *'  # 16:00 WIB
    - cron: '0 2 * * *' # 17:00 WIB
    - cron: '0 3 * * *' # 18:00 WIB
    - cron: '0 4 * * *' # 19:00 WIB

jobs:
  update-streak:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      
    - name: Update file to trigger commit
      run: |
        echo "Update on $(date)" >> streak.txt
        
    - name: Commit and push changes
      run: |
        git config --global user.name 'Adib-F'
        git config --global user.email 'fakhrimuhammadadib@gmail.com'
        git add streak.txt
        git commit -m "Keep streak green on $(date)"
        git push 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 HEAD:main
